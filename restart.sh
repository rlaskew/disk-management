#/bin/bash

# become root
# sudo -i

# unmount /data drive
# umount /data

# restore fstab to original state
# cp /etc/fstab.orig /etc/fstab

# remove /data directory
# rmdir /data

# remove and reattach extra nmve drive
# echo "use acg console to remove the (re) add nvme1n1 drive
